# type defination
# Declare a variable 'n' as an integer with value 5
n: int = 5

# Declare a variable 'name' as a string with value "Harry"
name: str = "Harry"

# Define a function 'sum' that takes two integers (a and b)
# and returns their sum (also an integer)
def sum(a: int, b: int) -> int:
    return a + b
'''
result = sum (4,5)
print(result)
'''